Put your own images here (logo, team photo, project/gallery photos).

The site currently uses:
  - The real DJS logo (loaded from the client's WSIMG URL) in the header & footer.
  - Unsplash PLACEHOLDER photos for the hero background, About section, and Gallery.

To use local images instead, drop them in this /assets/ folder and update the
matching src="" paths in index.html (and .hero__bg background URL in css/style.css).
